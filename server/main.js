import { Meteor } from 'meteor/meteor';

import "../imports/api/bbyProducts.js";
//importar esta puta mierda si no crea las collections ufkc
import "../imports/api/results.js";
Meteor.startup(() => {
  // code to run on server at startup
});
